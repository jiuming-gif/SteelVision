# SteelVision 简历项目描述

> 项目实际成果：检测精度 **94.5%**，单张图像推理耗时 **77.2ms**。
> 提供 AI/算法、后端开发、全栈开发 三个方向的版本。

---

# 一、AI/算法工程师方向

## 版本 A1：STAR法则完整版（推荐，约220字）

**项目名称：** 车辆零部件表面缺陷视觉检测系统（SteelVision）

**S (情境)：** 车辆零部件表面缺陷（夹杂物、补丁、划痕）长期依赖人工目检，效率低、一致性差，亟需自动化视觉检测方案。

**T (任务)：** 设计语义分割模型，对三类缺陷实现像素级精准识别，兼顾检测精度与推理速度以满足产线实时性要求。

**A (行动)：**
- 提出 HDI-UNet 模型，在 U-Net 编码器中引入 HDRAB 混合膨胀残差注意力模块，融合四条并行膨胀卷积分支（dilation rate 1-4）与通道注意力（CAB）、空间注意力（SAB），增强对细小缺陷的多尺度特征提取能力。
- 引入 Deepseek-V3 辅助判断机制，对模型检测结果进行二次校验，降低误检率。
- 设计 CrossEntropy + Dice Loss 联合损失函数，采用 RMSprop + ReduceLROnPlateau 优化策略，配合梯度裁剪与五种在线数据增强（亮度/对比度扰动、随机翻转/旋转/平移），提升模型鲁棒性。
- 构建 defect mIoU、F1 Score、混淆矩阵等多维度评估体系，基于验证集最佳 mIoU 与 Loss 双重策略保存模型。

**R (结果)：** 模型检测精度达 **94.5%**，单张图像推理耗时 **77.2ms**，满足实时检测需求；完整交付训练管线、模型推理与可视化评估，集成至 PyQt5 桌面客户端与 FastAPI 后端，支持单图/批量/摄像头三种推理模式。

---

## 版本 A2：STAR法则精炼版（约140字）

**项目名称：** 基于 HDI-UNet 与 Deepseek-V3 辅助判断的车辆零部件缺陷检测系统

**S：** 车辆零部件表面缺陷人工检测效率低、标准不统一。

**T：** 构建自动化语义分割系统，精准识别夹杂物、补丁、划痕三类缺陷。

**A：** 提出 HDI-UNet 模型，融合 HDRAB 混合膨胀残差注意力（多尺度膨胀卷积 + 通道注意力 + 空间注意力）；引入 Deepseek-V3 辅助判断机制进行二次校验；设计 CrossEntropy + Dice 联合损失与五种在线数据增强策略。

**R：** 检测精度达 **94.5%**，单张推理耗时 **77.2ms**，支持单图/批量/摄像头实时检测。

---

## 版本 A3：技术要点式（约80字，适合项目列表栏）

**车辆零部件表面缺陷视觉检测系统（PyTorch）**
- 提出 HDI-UNet 语义分割模型，融合 HDRAB 混合膨胀残差注意力（多尺度膨胀卷积 + CAB + SAB），结合 Deepseek-V3 辅助判断，检测精度 **94.5%**，推理耗时 **77.2ms**
- 设计 CrossEntropy + Dice Loss 联合损失与 RMSprop + ReduceLROnPlateau 优化策略，实现五种在线数据增强
- 构建 defect mIoU、F1 Score、混淆矩阵等评估体系，全链路交付（训练→推理→可视化→部署）

---

## 版本 A4：面试口述版（AI方向）

这个项目是为锐捷网络做的车辆零部件表面缺陷视觉检测系统，我主要负责算法部分。

问题是传统人工目检效率低、一致性差，我们需要对三种表面缺陷——夹杂物、补丁和划痕——做像素级的语义分割。

模型方面我提出了 HDI-UNet。为什么叫 HDI？核心改进在编码器端引入了 HDRAB 混合膨胀残差注意力块。标准 U-Net 对小缺陷的感受野有限，HDRAB 用四条并行的膨胀卷积分支，膨胀率 1 到 4，能同时捕获不同尺度的特征，通过残差连接融合后再接通道注意力 CAB 做特征重标定。此外还引入了 Deepseek-V3 作为辅助判断模块，对模型的分割结果做二次校验，有效降低了误检。

训练方面，损失函数用了 CrossEntropy 加 Dice Loss 的联合设计——Dice Loss 对类别不平衡敏感，交叉熵保证像素级准确。优化器用 RMSprop 配合 ReduceLROnPlateau，数据增强做了亮度对比度扰动、翻转、旋转、平移五种。评估体系我建得比较全，不止看 mIoU，还逐类追踪 IoU、Precision、Recall、F1 Score 和混淆矩阵，方便定位模型在哪些缺陷上表现差。

最终结果：检测精度 94.5%，单张图推理 77.2ms，满足产线实时性要求。整个模型最后集成到了 PyQt5 客户端和 FastAPI 后端里。

---

# 二、后端/全栈开发方向

## 版本 B1：STAR法则完整版（推荐，约220字）

**项目名称：** 车辆零部件表面缺陷视觉检测系统（SteelVision）— 后端与系统架构

**S (情境)：** 项目需要一个高可用的后端服务来承载 AI 模型推理、用户管理、社区互动与第三方 API 集成，同时要支撑桌面客户端与 Web 前端双端调用。

**T (任务)：** 设计并实现完整的后端系统架构，包括 RESTful API、数据库设计、AI 模型服务化部署，以及集成 Deepseek-V3 辅助判断与第三方服务（电商、地图）的 Agent 调度管线。

**A (行动)：**
- 基于 FastAPI 构建异步后端服务，设计分层架构（路由层 → 业务逻辑层 → 数据访问层），实现用户认证（bcrypt 密码哈希）、检测任务管理、社区内容等 RESTful API。
- 使用 SQLAlchemy ORM 设计 4 张核心数据表（User、DetectionTask、RepairAdvice、CommunityPost），并额外通过 PyMySQL 连接 MySQL 记录检测日志，实现双数据库协同。
- 设计 AI Repair Agent 模块，编排 Deepseek-V3 / Kimi / Doubao 三个 LLM 的自动降级调用链路，结合 Taobao API（维修材料搜索）与高德地图 API（附近修理厂导航）实现智能维修推荐。
- 使用 PyQt5 构建桌面客户端（约 1500 行），实现单图检测、批量检测、摄像头实时检测、AI 对话助手、管理员面板、明暗主题切换等完整功能，并通过 PyInstaller 打包为 Windows 可执行文件。

**R (结果)：** 后端支撑日均检测请求，系统支持 Windows 桌面客户端与 Web SPA 双端访问；模型检测精度 94.5%，单张推理耗时 77.2ms；AI Agent 多 LLM 降级机制保障服务在 API 不可用时的鲁棒性。

---

## 版本 B2：STAR法则精炼版（约150字）

**项目名称：** 基于 FastAPI 的车辆零部件缺陷检测系统 — 后端架构设计

**S：** 传统人工目检效率低，需要一套完整的后端系统支撑 AI 检测模型的服务化部署与多端调用。

**T：** 设计实现后端服务架构，覆盖 API 设计、数据持久化、模型服务化与第三方服务集成。

**A：** 基于 FastAPI + SQLAlchemy 构建分层后端架构，设计 4 张核心数据表与双数据库协同方案（SQLite + MySQL）；搭建 Deepseek-V3 / Kimi / Doubao 三 LLM 自动降级的 AI Agent 调度管线，集成 Taobao API 与高德地图 API；用 PyQt5 开发桌面客户端（约 1500 行）并 PyInstaller 打包为 exe。

**R：** 交付完整后端系统与桌面客户端，支持单图/批量/摄像头检测，检测精度 94.5%，单张推理 77.2ms。

---

## 版本 B3：技术要点式（约100字，适合项目列表栏）

**车辆零部件表面缺陷视觉检测系统 — 后端架构（FastAPI + PyQt5）**
- 基于 FastAPI 构建 RESTful API 服务，采用分层架构设计，SQLAlchemy ORM + SQLite/MySQL 双数据库协同，bcrypt 用户认证
- 设计多 LLM（Deepseek-V3 / Kimi / Doubao）自动降级的 AI Agent 调度管线，集成 Taobao + 高德地图第三方 API
- 用 PyQt5 开发全功能桌面客户端（约 1500 行），支持检测/AI 对话/管理面板/主题切换，PyInstaller 打包为 exe

---

## 版本 B4：面试口述版（后端方向）

这个项目是给锐捷网络做的车辆零部件缺陷检测系统，我负责后端架构和桌面客户端开发。

后端用的是 FastAPI。为什么选 FastAPI？因为项目需要异步处理 AI 模型推理——单张图 77ms 还好，但批量检测和摄像头实时模式对并发有要求，FastAPI 的 async/await 原生支持刚好匹配。整个后端我按分层架构设计的：路由层处理 HTTP 请求，业务逻辑层做检测调度和 Agent 编排，数据访问层用 SQLAlchemy ORM 统一管理。

数据库方面做了双数据库方案：SQLite 存用户、检测任务、社区内容这些业务数据，MySQL 单独记检测日志——因为日志写入频率高，和业务数据解耦后不会互相拖慢。

比较有意思的是 AI Agent 这块。做完缺陷检测之后，系统会自动判断维修方式——是自己修还是去修理厂。这个 Agent 对接了三个 LLM：Deepseek-V3 主力判断，Kimi 和 Doubao 做降级备用，任何一个挂了自动切下一个。判断完之后，如果是 DIY 维修就调 Taobao API 搜材料，如果要进厂就调高德地图搜附近的修理厂——这是典型的工具调用模式，跟 LangChain 的 Agent 思路一样，只是我没用框架，直接手写的调度逻辑。

桌面客户端用 PyQt5 写的，大概 1500 行。做了单张检测、文件夹批量、摄像头实时三种模式，还有 AI 对话助手、管理员面板、明暗主题切换。最后用 PyInstaller 打包成 exe 给客户直接用。

---

# 三、全栈开发方向

## 版本 C1：一段式概述（推荐，约280字）

针对传统人工目视检测效率低、精度差的问题，独立设计并实现了车辆零部件表面缺陷视觉检测系统 SteelVision 的全栈交付。算法层提出 HDI-UNet 语义分割模型，在 U-Net 编码器中融合 HDRAB 混合膨胀残差注意力与 Deepseek-V3 辅助判断机制，检测精度达 94.5%，单张推理仅 77.2ms。后端基于 FastAPI 构建异步 RESTful API 服务，采用分层架构设计，使用 SQLAlchemy ORM 管理 SQLite 与 MySQL 双数据库，实现用户认证（bcrypt）、检测任务调度与社区内容等核心业务模块，并设计多 LLM（Deepseek-V3 / Kimi / Doubao）自动降级的 AI 维修推荐 Agent，集成淘宝材料搜索与高德地图修理厂导航等第三方 API。前端使用 PyQt5 从零构建桌面客户端（约 1500 行），实现单图检测、批量检测、摄像头实时检测、AI 对话助手、管理员面板与明暗主题切换等功能，并通过 PyInstaller 打包为 Windows 可执行文件交付客户。全链路独立完成从模型训练、后端架构、数据库设计到桌面客户端开发与打包部署，具备完整的产品化落地能力。

## 版本 C2：精简版（约160字，适合空间紧张）

独立完成车辆零部件表面缺陷视觉检测系统 SteelVision 的全栈开发与交付。算法侧提出 HDI-UNet 模型（HDRAB 注意力 + Deepseek-V3 辅助判断），检测精度 94.5%，推理耗时 77.2ms；后端基于 FastAPI + SQLAlchemy 构建分层架构与双数据库方案，设计多 LLM 自动降级的 AI Agent 集成第三方 API；前端用 PyQt5 开发全功能桌面客户端（约 1500 行），支持三种检测模式、AI 对话与管理面板，PyInstaller 打包为 exe 交付。

## 版本 C3：面试口述版（全栈方向）

这个项目是我独立负责全栈开发的车辆零部件缺陷检测系统。从模型到后端到桌面客户端，整条链路都是我一个人做的。

先讲算法这块。核心问题是人工目检效率低、一致性差，需要自动识别三种表面缺陷。我提出了 HDI-UNet 模型，在 U-Net 里加了 HDRAB 注意力模块——本质是用四条不同膨胀率的并行卷积来抓不同尺度的缺陷特征，再结合 Deepseek-V3 做二次校验降低误检。最终精度 94.5%，推理 77ms，满足实时性要求。

后端用 FastAPI 搭的，选它是因为异步支持好，批量检测和摄像头实时模式对并发有要求。数据库方面做了 SQLite 加 MySQL 的双库方案——业务数据走 SQLite，高频检测日志单独走 MySQL，互不拖累。比较有意思的是 AI Agent 模块：检测完之后系统会自动判断修还是换，内部对接了三个 LLM 做降级容灾，Deepseek-V3 挂了自动切 Kimi，Kimi 再挂切 Doubao，然后根据判断结果调淘宝 API 搜材料或者调高德地图搜修理厂。

前端是 PyQt5 写的桌面客户端，大概一千五百行。做了单图检测、文件夹批量、摄像头实时三种模式，还有 AI 对话助手、管理员面板、明暗主题切换。最后用 PyInstaller 打了个 exe 包给客户直接用。

整个项目从模型训练到后端 API 到桌面客户端到打包部署全是我独立完成的，对我的全栈能力是一个比较完整的验证。

---

# 使用建议

## AI/算法方向

| 简历模块 | 推荐版本 |
|---------|---------|
| 项目经历（主区域） | A1（完整STAR） |
| 项目经历（空间紧张） | A2（精炼STAR） |
| 技能/项目速览栏 | A3（技术要点） |
| 面试准备 | A4（口述稿） |

**关键词标签：** `语义分割` `HDI-UNet` `HDRAB` `通道注意力` `Deepseek-V3` `Dice Loss` `PyTorch` `数据增强` `mIoU`

## 后端开发方向

| 简历模块 | 推荐版本 |
|---------|---------|
| 项目经历（主区域） | B1（完整STAR） |
| 项目经历（空间紧张） | B2（精炼STAR） |
| 技能/项目速览栏 | B3（技术要点） |
| 面试准备 | B4（口述稿） |

**关键词标签：** `FastAPI` `SQLAlchemy` `RESTful API` `PyQt5` `AI Agent` `LLM调度` `双数据库` `PyInstaller` `高德API` `MySQL`

---

## 全栈开发方向

| 简历模块 | 推荐版本 |
|---------|---------|
| 项目经历（主区域） | C1（一段式概述） |
| 项目经历（空间紧张） | C2（精简版） |
| 面试准备 | C3（口述稿） |

**关键词标签：** `FastAPI` `PyQt5` `PyTorch` `HDI-UNet` `SQLAlchemy` `Deepseek-V3` `AI Agent` `PyInstaller` `全栈开发`

---

## 投递策略建议

建议根据岗位准备不同侧重点的简历，同一项目写不同版本：

- **AI 岗简历**：用 A 系列版本，项目名称强调 "HDI-UNet 模型"，内容突出模型创新、训练策略、指标
- **后端岗简历**：用 B 系列版本，项目名称强调 "系统架构"，内容突出 API 设计、数据库、Agent 调度
- **全栈岗简历**：用 C 系列版本，项目名称强调 "全栈交付"，内容突出独立完成全链路的能力

所有方向都保留核心数据（94.5%精度、77.2ms推理耗时），这是最有说服力的量化成果。
